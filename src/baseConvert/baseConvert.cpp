#include "util.hpp"

#include <string>

auto baseConvert(const std::string& inputStr, const std::string& baseStr)
{
    int base = std::stoi(baseStr);
    if (base > 36)
        return "";
    return "";
}

int main() {}
