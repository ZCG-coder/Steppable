{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Multiply",
            "program": "${workspaceFolder}/build/multiply",
            "args": ["56", "76", "+profile"],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: build"
        }
    ]
}